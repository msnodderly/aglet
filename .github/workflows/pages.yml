name: Deploy manual to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/src/**'
      - 'docs/images/**'
      - 'docs/templates/**'
      - 'docs/Makefile'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pandoc + TeX (xelatex) + fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pandoc \
            texlive-xetex texlive-fonts-recommended texlive-fonts-extra \
            texlive-latex-extra fonts-ibm-plex
      - name: Build manual (HTML + Markdown + typeset PDF)
        # Local authoring uses Palatino; CI substitutes a TeX Live equivalent.
        run: |
          make -C docs all \
            MAINFONT="TeX Gyre Pagella" \
            MONOFONT="IBM Plex Mono"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_site
      - id: deployment
        uses: actions/deploy-pages@v4
