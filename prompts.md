# Prompts

Canonical agent workflow prompt lives at [prompt.md](/Users/mds/src/aglet-ec938313-atomic-claim/prompt.md).

Use the atomic claim step from that prompt:

```bash
cargo run --bin agenda-cli -- --db aglet-features.ag claim <ITEM_ID>
```
