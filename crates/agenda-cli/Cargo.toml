[package]
name = "agenda-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
agenda-core = { path = "../agenda-core" }
agenda-tui = { path = "../agenda-tui" }
clap = { version = "4", features = ["derive", "env"] }
chrono = "0.4"
uuid = "1.11"
rust_decimal = "1.36"
